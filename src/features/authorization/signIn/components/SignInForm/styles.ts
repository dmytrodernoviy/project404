import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    height: '45%',
    justifyContent: 'space-between',
    marginBottom: '40%',
    width: '100%'
  }
});
